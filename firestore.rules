rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /survey/total {
      allow read: if true;
      allow write: if request.auth != null;
    }
    match /voters/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
    match /platformPoll/votes {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
